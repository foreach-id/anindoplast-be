NODE_ENV=development
PORT=3000
APP_NAME=API-order

# Database Configuration
DATABASE_URL="mysql://root:@localhost:3306/db_order_mgmt"
DB_HOST=localhost
DB_PORT=3306
DB_NAME=db_order
DB_USER=root
DB_PASSWORD=
DB_DIALECT=mysql

# JWT Configuration
JWT_SECRET=changeme_secret_key
JWT_EXPIRE=30m
JWT_REFRESH_SECRET=changeme_refresh_secret_key
JWT_REFRESH_EXPIRE=7d

# CORS Configuration
# Single origin: CORS_ORIGIN=http://localhost:3000
# Multiple origins (comma-separated): CORS_ORIGIN=http://localhost:3000,http://localhost:5173
# Allow all origins (wildcard): CORS_ORIGIN=*
# Note: Using * will automatically disable credentials for security
CORS_ORIGIN=http://localhost:3000

# Cookie Configuration
# Set to 'false' to disable secure cookies in production (for HTTP testing)
# Default: true in production, false in development
# COOKIE_SECURE=false

# Bcrypt
BCRYPT_SALT_ROUNDS=10

# FRONTEND URL Configuration
FRONTEND_URL=http://localhost:5173

# SMTP CONFIGURATION DEFAULT
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=dewandrarb@gmail.com
SMTP_PASS=chhvhhzxyhibzmsh 
SMTP_FROM="FotoGenie Support <no-reply@fotogenie.com>"

# KIRIMINAJA API CONFIGURATION
# "sandbox" untuk development, "production" untuk live
KIRIMINAJA_ENV=sandbox

# API Key dari dashboard KiriminAja (Integrasi menu)
KIRIMINAJA_API_KEY=your_api_key_here

# SHIPPER / PENGIRIM CONFIG
# Info pengirim (toko kamu) – dipakai saat buat order ke KiriminAja
SHIPPER_NAME=Nama Toko Kamu
SHIPPER_PHONE=6285271372389
SHIPPER_DISTRICT_ID=14046   # District ID KiriminAja (cek via /api/shipping/district/search)
SHIPPER_ADDRESS=Jl. Contoh No. 1, Kelurahan, Kecamatan
SHIPPER_ZIP=12345